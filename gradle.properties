# Increase JVM heap for Gradle (avoids "Java heap space" during remapping)
org.gradle.jvmargs=-Xmx3g -Xms512m -Dfile.encoding=UTF-8

# Reduce parallel workers to limit memory usage on CI
org.gradle.workers.max=2
