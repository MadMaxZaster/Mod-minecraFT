pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven { url = uri('https://maven.fabricmc.net/') }
    }
}

rootProject.name = 'modminecraft'

settings.gradle.beforeProject { project ->
    // Workaround for older gradle versions
}
